你是一名负责机器人视觉、算法评测平台和软件工程的资深工程师。请直接在当前工作区实现一个用于“智能交通创新实践”课程的四轮车视觉寻迹实验项目。

本任务要求实际编写代码、运行系统、执行测试并修复问题，不是只输出方案、目录结构、伪代码或前端演示页面。

优先完成可靠、可扩展的测试平台，再开发独立封装的参考算法。目标交付两套算法：模块化视觉寻迹算法，以及从图像直接输出转向角和速度的端到端学习算法。先确保平台和模块化算法完整可用，再完成端到端算法。

一、课程背景与核心任务

小车搭载一个前向摄像头，需要根据摄像头图像完成：

1. 初始时小车不一定在线上，也不一定朝向引导线。
2. 初始画面中能看到目标引导线和起点区域。
3. 小车需要识别目标线，接近起点或合法接入区域，将行进方向与引导线方向对齐，然后连续沿线行驶。
4. 目标引导线在物理空间中连续，但可能包含大幅度转弯、连续弯、回头弯和在视野内反复出现的不同线段。
5. 视野内可能同时出现多条线，包括与当前引导线完全不相连的干扰线。
6. 小车不能因为另一条线更近、更居中、更直或更清晰，就切换到那条不相连的线上。
7. 算法需要利用图像中的几何结构和时间连续性，持续跟踪当前引导线。

本项目重点是视觉寻迹、目标连续性和算法评测，不涉及真实车辆的电机驱动、PWM、串口、CAN、底盘固件或硬件闭环调参。

但必须保留最小的运动仿真和可替换执行适配器，使算法输出能够改变下一帧摄像头图像，从而验证真正的连续运动效果。

二、先明确并记录任务假设

请将以下默认假设写入配置和文档，不要把未定义问题偷偷固化在代码中。

1. 车辆模型

默认采用四轮阿克曼转向车辆，以运动学自行车模型进行仿真。

配置至少包括：
- 轴距、轮距和车辆尺寸；
- 最大等效前轮转角；
- 最大速度、加速度和制动减速度；
- 转角变化率限制；
- 摄像头安装位置、朝向、视场角和分辨率；
- 仿真时间步长。

默认动作定义：
- steering_angle_rad：等效前轮转向角，单位 rad；
- speed_mps：目标行进速度，单位 m/s。

必须明确转向正负号、速度正负号和车辆坐标系。

不得把 steering_angle_rad 当作偏航角速度，也不得让阿克曼车辆在速度为零时原地改变车身朝向。默认不允许倒车；如果提供倒车模式，必须单独配置和评测。

“先对准引导线”应通过符合车辆运动学约束的接近和转弯实现，不能直接修改车辆位置或朝向。

2. 起点和目标线识别

多条线同时出现时，必须有明确的目标指定机制。

默认提供可从图像识别的起点标记和行进方向标记，标记样式可配置。另提供人工在首帧点击目标线或框选起点区域的初始化方式。

初始化提示属于公开的任务输入，必须在实验配置和结果中记录。不得通过隐藏的地图 ID 或目标线真值自动告知算法答案。

如果既没有可辨识标记，也没有初始化提示，并且图像存在多个同等合理的候选目标，算法应报告 AMBIGUOUS 或请求初始化，不能声称一定能判断用户想跟踪哪条线。

不同算法比较时必须使用相同的初始化条件。

3. 连通性与可观测性

区分：
- 世界地图中的物理连通性；
- 当前图像中的可见连通性；
- 历史观测支持的目标连续性。

同一条连续曲线可能因为视野裁剪或短暂遮挡，在图像中表现为多个不相连片段。不能只凭当前帧的连通域编号判断目标是否相同。

反过来，也不能通过大范围形态学闭运算，随意把两条真实断开的线连接起来。

默认核心场景没有真实分叉或无标记交叉路口。可将交叉、分叉、视觉上不可区分的重叠情况作为扩展压力场景，但必须标明额外任务规则和可观测性限制。

4. 大转弯与物理可行性

核心验收场景必须满足车辆转弯能力和允许的跟踪误差范围。

对曲率超过车辆能力、零半径尖角或无法接入的初始姿态，应将其作为单独的不可行压力场景，测试算法是否能报告不确定性、减速或停止。

不得通过车辆瞬移、超限转角或放宽评测规则，让不可行场景看起来通过。

三、交付顺序与技术选型

按照以下顺序完成，不要为了复杂模型而牺牲平台可靠性：

阶段 A：公共接口、场景格式和最小运行链路。
阶段 B：完整测试平台、可视化、回放和评测。
阶段 C：模块化算法及其调试和优化。
阶段 D：端到端算法、数据生成、训练和公平对比。
阶段 E：完整验收、文档和交付整理。

每个阶段都需要有可执行结果，并更新项目进度文档。

默认采用：
- Python 后端和算法 SDK；
- FastAPI 提供 HTTP API；
- WebSocket 或其他合适机制提供实时状态；
- React + TypeScript + Vite 前端；
- Canvas 或 SVG 完成二维可视化；
- NumPy、OpenCV 等实现图像处理和轻量仿真；
- PyTorch 作为端到端算法的可选依赖。

如当前仓库已有合适技术栈，可以沿用并说明理由。不要无必要引入 ROS、Gazebo、微服务集群或复杂云服务。

必须实现本地运行版本。学生完成依赖安装后，应能通过一个明确命令启动，再用浏览器访问。

优先兼容 Windows 和 Linux；未实际测试的系统必须说明，不能声称已经验证。

平台、模块化算法和默认演示必须可以在普通 CPU 环境中运行。学习算法依赖应独立安装，不能让未安装 PyTorch 的用户无法启动平台。

可以提供 Docker 部署方案，但 Docker 不能成为本地使用的唯一方式。公网部署属于可选扩展，不是本次必需交付。

四、平台必须支持的三种运行模式

1. 单帧调试

支持上传图片、指定算法和参数，显示：
- 算法输入；
- 识别到的线；
- 目标选择；
- 局部路径或动作输出；
- 置信度；
- 中间调试结果；
- 耗时和错误信息。

2. 视频或图像序列回放

支持读取视频和按时间排序的图像序列，保留算法跨帧状态。

支持暂停、逐帧查看和结果回放。

必须明确：录制视频的后续画面不会因为算法动作而改变，因此该模式用于感知和时序分析，不能直接证明闭环行驶成功。

跳转帧时，应展示保存的结果，或按明确规则重置并重新推进算法状态，不能带着来自未来帧的状态继续计算。

3. 闭环仿真

必须形成真实链路：

车辆状态
→ 摄像头图像渲染
→ 算法推理
→ 动作或局部路径
→ 执行适配器
→ 车辆状态更新
→ 下一帧图像。

不能预先播放固定视频，也不能用地图真值直接驱动车辆后宣称算法完成寻迹。

仿真采用固定时间步长。算法计算耗时、仿真时间和界面刷新时间应分开管理，避免不同电脑的界面帧率改变车辆运动结果。

提供确定性评测模式，以及可配置延迟、掉帧和动作超时的压力测试模式。明确各模式在推理超时时如何处理动作。

五、场景系统与摄像头模拟

1. 场景表示

使用可扩展的 JSON 或 YAML 格式，支持：
- 有序的目标路径几何；
- 起点、终点和行进方向；
- 多条互不相连的干扰线；
- 初始车辆位置与朝向；
- 线宽、颜色、背景和照明参数；
- 摄像头参数；
- 车辆参数；
- 场景随机种子；
- 用于评测的私有真值。

曲线宜采用折线、样条或其他容易生成和编辑的表示。

必须明确地图长度单位、坐标系和路径方向。

2. 最低场景覆盖

至少提供以下场景族，并支持每类生成多个随机实例：
- 直线；
- 单弯；
- S 形和连续弯；
- 大角度转弯；
- 车辆可通过的回头弯；
- 目标线旁有平行干扰线；
- 目标线与干扰线距离很近但不相连；
- 同一条目标线的不同部分同时进入视野。

在这些场景上组合：
- 车辆初始不在线上；
- 初始朝向存在偏差；
- 干扰线比目标线更靠近画面中心；
- 线宽和对比度变化；
- 阴影、噪声和模糊；
- 分辨率和摄像头安装角变化；
- 目标线暂时部分离开视野。

遮挡和严重掉帧可以作为压力测试，需与核心场景分开统计。

提供场景合法性检查。生成核心场景时验证起点可见、目标指定明确、几何约束合理；不能根据某个被测算法是否跑得过来决定场景是否合法。

3. 摄像头渲染

使用地面平面和可配置相机模型，生成带透视效果的前向视图。

算法只能接收正常摄像头图像及公开的相机标定参数，不能接收俯视真值图。

支持可复现的光照、颜色、噪声和模糊扰动。

相机投影和反投影应有数值一致性测试。涉及米制路径时必须依赖有效标定，不能把像素坐标直接当作米。

仿真效果应足够用于课程算法开发，但文档必须说明其与真实摄像头和真实车辆之间的差异。

六、平台与算法必须严格解耦

推荐结构如下，可按实际工程需要调整：

project/
  sdk/                    公共数据类型、协议和算法接口
  backend/                API、任务管理和结果存储
  frontend/               可视化界面
  simulator/              地图、相机和运动仿真
  evaluation/             指标、批量测试和报告
  execution_adapters/     局部路径到仿真动作的参考适配器
  algorithms/
    minimal_example/      最小算法接入示例
    modular_tracker/      模块化寻迹算法
    end_to_end_policy/    端到端算法
  student_template/       学生算法模板
  scenarios/              场景和生成配置
  tests/                  测试
  scripts/                安装、启动、评测和训练入口
  docs/                   接口、架构和课程使用文档
  artifacts/              运行结果、日志和模型产物

关键约束：
- 平台通过 SDK 和注册机制加载算法。
- 算法不能依赖前端、后端内部对象或仿真器私有实现。
- 添加新算法无需修改平台核心代码。
- 切换算法、切换场景和创建新运行时，必须重置算法状态。
- 删除某套参考算法后，平台仍能启动并加载其他算法。
- 学生应能只复制模板、实现接口并注册算法，即可完成测试。
- 测试数据生成器、训练专家和评分器可以使用真值，但部署推理算法不可以。

七、公共算法接口

先设计并实现明确的数据协议、类型校验和版本字段，再开发具体算法。

至少提供类似以下生命周期：

initialize(config, public_context)
reset(initial_observation, task_hint)
step(observation) -> algorithm_output
close()

支持有状态算法；每个实验运行使用独立实例。

1. Observation

至少包括：
- protocol_version；
- episode_id；
- frame_id；
- timestamp_s；
- dt_s；
- RGB 图像；
- 图像尺寸、颜色空间和数据类型；
- 可选的相机标定；
- 明确声明的初始化提示。

默认纯视觉赛道不提供真实车辆位置、真实朝向、全局目标路径、目标线真值 ID、隐藏地图或未来图像。

可以支持扩展遥测赛道，但必须声明具体可用传感器及其噪声模型，并与纯视觉赛道分开评测。

算法可使用历史图像、内部状态和自身历史输出。

2. AlgorithmOutput

至少支持三种能力声明：

A. perception：输出图像中的线、掩膜或中心线，用于感知评测；
B. path：输出车辆坐标系下的局部有序路径，由公共执行适配器完成仿真；
C. action：直接输出 steering_angle_rad 和 speed_mps。

允许算法同时输出路径、动作和调试信息，但运行模式必须明确采用哪一个输出作为执行依据。

输出可包含：
- 状态；
- 置信度；
- 目标中心线；
- 局部路径；
- 横向偏差、航向偏差和曲率估计；
- 转向角和速度；
- 调试图像或结构化中间结果；
- 诊断信息。

所有可选字段需明确含义。字段缺失不能被悄悄当成零。

状态至少能表达：
UNINITIALIZED、ACQUIRE、ALIGN、TRACK、LOST、
AMBIGUOUS、FINISHED、ERROR。

明确图像坐标系、车辆坐标系、点的排序、单位、数值范围和无效值处理。

3. 执行适配器

对 path 模式提供独立、可替换的参考路径执行器，例如基于前视点的 Pure Pursuit，并根据曲率和置信度调节速度。

它只消费算法输出的局部路径和允许使用的车辆状态，不能读取目标线真值。

感知指标与闭环表现分开报告，避免把执行器的问题误判为视觉算法问题。

4. 接入形式

至少实现：
- Python 算法插件；
- 基于标准输入输出或等价进程通信的独立进程适配器。

提供协议和示例，使其他语言或独立 Python 环境的算法能够接入。

协议必须有明确的消息边界、图像编码、最大消息大小、超时、错误响应和版本检查。不得通过不受限制的反序列化执行传入对象。

八、平台可靠性与可视化

1. 运行可靠性

被测算法应在独立 worker 进程中运行，避免崩溃或死循环拖垮 Web 服务。

处理：
- 初始化失败；
- 推理异常；
- 超时和卡死；
- 非法 JSON 或协议版本；
- NaN、Inf 和错误数组形状；
- 超范围动作；
- 图像损坏；
- 空结果；
- 任务取消；
- 浏览器断线；
- 并发运行之间的状态污染。

worker 超时后必须可以终止并清理，后续实验仍能正常启动。

对异常采取明确策略，例如结束当前回合或触发安全停车。记录算法原始输出、实际执行动作和干预原因。

不能用静默纠正或隐藏替代算法掩盖被测算法失败。

任务状态至少包括：
queued、running、completed、failed、cancelled。

采用有界队列，避免图像、日志和 WebSocket 消息无限积压。用户关闭页面后，服务不能无提示地无限运行实验。

本地版默认只执行用户明确安装或配置的算法。不要附带一个直接在公网主服务执行任意上传 Python 代码的接口。

独立进程只是故障隔离，不要宣称它是面向恶意代码的安全沙箱。若另行实现在线提交，需采用独立的隔离执行方案。

2. 可视化界面

使用清晰的中文界面，至少包括：
- 输入来源、场景和算法选择；
- 随机种子和算法参数；
- 启动、暂停、单步、停止和重置；
- 原始摄像头画面；
- 算法叠加画面；
- 场景俯视图和车辆实际轨迹；
- 预测局部路径和历史跟踪结果；
- 当前状态、置信度、速度、转向和耗时；
- 偏差、速度和转角随时间变化的曲线；
- 运行日志和具体错误；
- 回放与结果导出；
- 多算法评测结果对比。

俯视图和评分使用的真值必须标注为“评测/调试信息”，不能混入算法输入。

不适用于某类算法的指标或调试面板显示“不提供”，不能伪造数据。

端到端算法没有可解释中心线输出时，允许只展示动作、置信度及其实际提供的调试信息。

3. 结果保存

每次运行保存：
- 场景配置和种子；
- 算法名称、版本和参数；
- 协议及评测器版本；
- 依赖环境；
- 每帧输出、执行动作和事件；
- 指标；
- 可重放数据；
- 失败原因。

支持 JSON 和 CSV 导出。大体积图像记录应可配置，避免默认无限占用磁盘。

九、模块化参考算法

实现一套实际可用、可解释、CPU 可运行的参考算法。不要只用“每帧寻找最大轮廓”或“寻找画面中心最近黑线”作为完整方案。

建议模块如下，允许选择更合适的方法，但必须解释设计依据：

1. 图像预处理
- 颜色或亮度归一化；
- 抗噪声处理；
- 可配置的线条分割；
- 合理的 ROI；
- 有标定时进行地面视角转换；
- 保留有效区域和不确定性。

2. 线结构提取
- 连通域、骨架或中心线提取；
- 将候选线段表示为有序路径或图结构；
- 保留曲率、端点、方向和候选关系；
- 避免过度平滑抹掉急弯；
- 避免将相邻但断开的线错误合并。

3. 初始化和接入
- 从起点标记或公开提示确定目标；
- 判断允许的行进方向；
- 生成可实现的接近和对齐路径；
- 用明确条件在 ACQUIRE、ALIGN 和 TRACK 之间切换。

4. 时序目标关联
综合考虑：
- 上一帧目标的预测位置；
- 方向与切向连续性；
- 图结构和路径连续性；
- 形状及曲率一致性；
- 可见片段与历史目标之间的对应；
- 合理的运动范围。

维护算法内部目标身份，不能每帧重新选择最靠近中心的线。

发生歧义时可以保留多个候选假设，但不能无依据切线。

5. 急弯处理
- 沿目标路径的顺序提取前方局部路径；
- 避免跨越回头弯两侧或跳到同一路径上更远的分支；
- 根据曲率调整前视范围和建议速度；
- 保持输出连续，并尊重车辆能力。

6. 丢失与恢复
- 短时缺失可依据历史状态预测，但预测时长和置信度衰减必须有界；
- 恢复时核验目标身份；
- 不确定时减速或停止；
- 不允许靠切换到邻近干扰线伪装恢复成功。

提供主要参数说明、中间结果可视化，以及针对“时序关联”和“连通性约束”的消融实验。

十、端到端参考算法

实现独立封装的学习型算法，其部署推理流程为：

当前图像或历史图像序列
→ 神经网络
→ 转向角和速度。

模型不能在推理时调用模块化算法生成主要动作，再将其包装成“端到端”。

1. 模型

选择适合课程和普通计算资源的轻量结构，例如图像编码器加帧堆叠或循环状态。

必须考虑时间信息，因为单帧可能无法区分相邻线和当前目标身份。

说明输入预处理、历史长度、状态重置、动作归一化、输出范围以及置信度的来源。不能把常数包装成可信的置信度估计。

2. 训练链路

交付实际可运行的：
- 数据生成；
- 专家轨迹采集；
- 数据校验；
- 数据集划分；
- 训练；
- 验证；
- checkpoint 保存和加载；
- 独立推理；
- 闭环评测。

可以采用模仿学习，专家来自模块化算法或训练专用的真值规划器。

训练专家使用真值时，必须明确标注特权信息，仅用于生成训练监督，不能进入部署推理接口。

如果普通行为克隆在闭环中出现误差积累，可实现数据聚合和纠偏训练。

3. 数据覆盖

覆盖：
- 正常沿线；
- 偏离后的回归；
- 起点接近和对齐；
- 多线干扰；
- 急弯与回头弯；
- 不同初始姿态；
- 光照、颜色、纹理、噪声和相机参数变化；
- 歧义或丢失情况下的减速、停止样本。

训练、验证和测试按完整地图或场景组划分，不能随机拆分同一视频的相邻帧后声称泛化良好。

增强参数只能在合理范围内使用；测试地图和测试种子不能参与训练调参。

4. 模型交付真实性

至少实际运行一次训练和独立推理验证，并记录真实环境、耗时、训练量和指标。

提供小规模 CPU 验证配置，以及可扩大数据量和训练时长的正式配置。

如果资源允许，应完成有实际寻迹能力的训练并保存可用权重。

如果只能完成流程验证，必须明确标注权重尚未达到性能目标，不能把随机权重、仅完成几步训练的模型或专家算法回退结果宣称为成熟端到端算法。

缺少 checkpoint 时，界面应明确说明模型不可用并给出训练命令，不能悄悄启用随机策略。

如果配置公共限幅或安全停车机制，必须同时报告原始策略结果、实际执行结果和干预次数。

十一、评测与防止“虚假成功”

1. 信息隔离

真值只能供：
- 场景生成；
- 仿真；
- 评分；
- 明确标注的训练专家；
- 调试可视化。

推理算法不得得到：
- 目标线真值 ID；
- 全局真值路径；
- 精确真实位姿；
- 未来图像；
- 隐藏地图参数；
- 从文件名、场景编号或随机种子反查答案的通道。

通过接口检查和集成测试验证信息边界。不要把本地进程隔离夸大为能防止恶意用户读取所有本地文件。

2. 指标

至少包括：
- 初始化和接入成功率；
- 接入耗时；
- 沿目标路径的有效完成比例；
- 整体成功率；
- 横向误差的均值、P95 和最大值；
- 航向误差；
- 非法换线次数和发生位置；
- 丢失次数、丢失时长和恢复成功率；
- 推理耗时的均值、P95 和超时比例；
- 实际行进速度和完成时间；
- 动作饱和、转向抖动和安全干预次数；
- 崩溃、协议错误和非法输出次数。

每个指标必须有定义、单位、适用条件和聚合方式。

未标注真实路径的视频不能计算真实横向误差；没有标定的图像不能输出伪造的米制误差。

3. 连续进度和换线判定

评测器根据私有目标路径、有序弧长和历史投影判断进度，不能只取整张地图上最近的点。

防止：
- 跳到回头弯的另一侧后进度突增；
- 从起点直接穿越到终点；
- 沿干扰线行驶后靠近终点被计为成功；
- 原地停车取得很小误差而被计为优秀；
- 只看最后一帧位置就判断任务完成。

接入阶段与正常跟踪阶段应分别评分。

非法换线应结合真实路径身份、车辆轨迹、持续时间和阈值判定，避免单帧数值抖动导致误报。

定义明确的成功、超时、偏离、换线、不可行和算法失败条件。

4. 公平比较

两套算法使用相同的：
- 场景和种子；
- 车辆与相机参数；
- 初始化信息；
- 观测权限；
- 动作边界；
- 超时策略；
- 评分器。

不同输出模式或额外传感器权限需分组报告。

在批量评测前确定核心场景、压力场景和指标。不得在看到结果后删掉失败场景，或通过放宽阈值掩盖失败。

以模块化算法在独立可行测试集上达到至少 95% 成功率、零非法换线作为优化目标。该数值是目标，不是可预先声称已达到的结果。

若未达到，报告实际结果并分析失败原因。不能宣称算法“最优”或适用于所有真实环境。

十二、测试与验收

至少实现并实际执行以下测试：

1. 平台链路
- 干净环境安装和启动；
- 前后端构建及基本交互；
- 单帧、序列和闭环模式；
- 任务取消和重置；
- 算法切换；
- 结果保存、回放和导出。

2. 公共接口
- 协议序列化和版本校验；
- RGB/BGR、数组形状和单位；
- 坐标变换；
- 空结果和缺失字段；
- 非法动作和超范围输出。

3. 仿真与评分
- 相机投影一致性；
- 车辆运动学、转角限制和时间步长；
- 速度为零时不发生虚假原地转向；
- 同种子复现；
- 有序路径进度；
- 跳到相邻线、回头弯另一侧、终点捷径等反作弊回归案例；
- 无标注数据不产生真值指标。

4. 算法故障
用专门的测试插件模拟异常、超时、死循环、NaN 和协议错误，验证：
- 当前任务被正确标记；
- worker 可以清理；
- 服务仍响应；
- 下一次实验可以正常运行。

5. 算法行为
覆盖离线起步、大朝向偏差、急弯、回头弯、多条平行线、近距离断开线和短时丢失。

6. 批量评测
提供快速回归集和较大的独立评测集。

建议快速集覆盖至少 8 类场景；完整报告使用每类多个种子，并列出样本量、成功数和失败类型，不能只展示最佳的一次运行。

训练或调参中使用过的场景应标注为训练或验证数据，不得继续称为未见测试集。

十三、必须交付的文件和使用体验

交付：
- 完整可运行代码；
- 清晰的项目结构；
- 依赖声明和必要的版本锁定；
- 自动化安装或环境检查脚本；
- 本地启动入口；
- 命令行批量评测入口；
- 两套独立算法目录；
- 学生模板和最小接入示例；
- 场景示例及生成器；
- 测试代码；
- 实际运行得到的样例结果；
- 中文 README；
- 接口说明；
- 架构说明；
- 学生接入教程；
- 教师评测说明；
- 真实的 benchmark 报告；
- 端到端训练说明和模型状态；
- 已知问题及适用范围。

学生正常使用流程应为：
1. 按 README 安装；
2. 用一个命令启动；
3. 浏览器选择示例场景并运行；
4. 复制算法模板、实现接口并注册；
5. 切换到自己的算法；
6. 查看结果并导出报告。

教师应能通过明确命令批量测试多个算法，得到可复现、可比较的结果。

所有文档命令必须与真实代码一致，并尽可能实际执行验证，不能编造尚未实现的 CLI。

十四、执行方式与完成标准

开始时：
- 检查当前目录、已有代码和仓库规则；
- 不覆盖无关文件；
- 确认运行环境；
- 写出简短实施计划；
- 将任务假设、接口约定和验收条件保存到项目文档；
- 随后直接开始实现。

对一般工程选择，使用合理默认值并记录理由，不必反复询问。

只有缺失信息会根本改变任务定义时才提问；提问期间继续完成不受影响的工作。

不要将精力主要花在页面装饰上。平台正确性、算法接入便利性、信息隔离和评测可信度优先。

不要以空类、TODO、硬编码结果、固定动画、伪造指标或没有使用到的模型替代核心功能。

每个阶段完成后执行对应检查，发现问题先修复再推进。

如果环境限制导致某部分无法执行，要写明：
- 已实现的内容；
- 已运行的内容；
- 尚未验证的内容；
- 具体限制；
- 可以继续执行的准确命令。

不要把“代码已经写出”当成“测试已经通过”，也不要把“训练脚本能启动”当成“学习算法已有良好效果”。

最终交付说明至少包含：
1. 项目实际完成情况；
2. 安装和启动命令；
3. 学生接入算法的最短步骤；
4. 两套算法的实现和模型状态；
5. 已执行测试及真实结果；
6. 批量评测指标和典型失败案例；
7. 已知限制与后续改进方向。

请从检查工作区和建立接口规范开始，然后逐步完成这个项目。